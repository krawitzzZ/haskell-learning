
module Monads.EitherError where


-- instance (Error e) => Monad (Either e) where
--   return x = Right x
--   Right x   >>= f = f x
--   Left  err >>= f = Left err
--   fail msg = Left (strMsg msg)

-- TODO: rewrite pole and birds excercise to store the amount of birds on each side
